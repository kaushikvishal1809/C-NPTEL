1) Explore some other commands like -E, + and ?
Ans)
    -E denotes extended regular expressions, it interprets pattern as extended regular expressions.
    + is a repetetion operator, the preceeding item will be matched one or more times.
    ? is a repetetion operator, the preceeding operator is optional and matched at most once.